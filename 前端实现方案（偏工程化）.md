# 前端实现方案（偏工程化）

- 技术选型建议
- CSS设计体系（变量+基础样式）
- 动效实现（关键动画代码）
- 各页面组件实现思路

👉 你可以直接丢给前端开发开干。

------

# ✅ 一、技术选型（推荐）

### 框架

- React / Vue（任选）
- 推荐：**React + Vite**

### 动效库

- 基础：CSS + requestAnimationFrame
- 进阶（强烈推荐）：
  - **GSAP（高质量动效）**
  - 或 **Framer Motion（React）**

### UI实现

- 不用传统UI库（会降低科技感）
- 用：**Tailwind + 自定义样式**

------

# ✅ 二、全局CSS设计系统（核心）

```css
:root {
  --bg-main: #0B1020;
  --bg-panel: rgba(255,255,255,0.04);
  --border-glow: #00E5FF;
  --text-main: #E6F7FF;
  --text-dim: rgba(255,255,255,0.6);

  --color-primary: #00E5FF;
  --color-secondary: #4D8BFF;
  --color-success: #00FFA3;
  --color-error: #FF4D6D;
}
```

------

# ✅ 三、全局背景（科技感核心）

```css
body {
  background: radial-gradient(circle at 20% 20%, #0f1a3a, #050814);
  color: var(--text-main);
  overflow: hidden;
}
```

------

## 🌌 动态网格背景

```css
.bg-grid {
  position: fixed;
  inset: 0;
  background-image: 
    linear-gradient(rgba(0,229,255,0.05) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,229,255,0.05) 1px, transparent 1px);
  background-size: 40px 40px;
  animation: gridMove 20s linear infinite;
}

@keyframes gridMove {
  from { transform: translateY(0); }
  to { transform: translateY(40px); }
}
```

------

## ✨ 粒子流（轻量实现）

```css
.particle {
  position: absolute;
  width: 2px;
  height: 2px;
  background: #00E5FF;
  opacity: 0.6;
  animation: float 6s linear infinite;
}

@keyframes float {
  from {
    transform: translateY(0);
    opacity: 0;
  }
  to {
    transform: translateY(-200px);
    opacity: 1;
  }
}
```

👉 建议JS生成20~40个即可

------

# ✅ 四、面板（科技卡片）

```css
.panel {
  background: var(--bg-panel);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(0,229,255,0.2);
  border-radius: 12px;
  position: relative;
  overflow: hidden;
}

/* 发光边框 */
.panel::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: 12px;
  border: 1px solid transparent;
  background: linear-gradient(120deg, transparent, #00E5FF, transparent) border-box;
  -webkit-mask:
    linear-gradient(#fff 0 0) padding-box,
    linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  animation: borderFlow 3s linear infinite;
}

@keyframes borderFlow {
  0% { opacity: 0.3; }
  50% { opacity: 1; }
  100% { opacity: 0.3; }
}
```

------

# ✅ 五、页面1：题型选择（卡片动效）

```css
.type-card {
  transition: all 0.3s ease;
  cursor: pointer;
}

.type-card:hover {
  transform: translateY(-6px) scale(1.03);
  box-shadow: 0 0 20px rgba(0,229,255,0.6);
}

.type-card.active {
  box-shadow: 0 0 40px rgba(0,229,255,1);
}
```

------

## 点击“充能”动画

```css
@keyframes energyFill {
  0% { box-shadow: 0 0 0 rgba(0,229,255,0); }
  100% { box-shadow: 0 0 40px rgba(0,229,255,1); }
}
```

JS触发：

```js
el.classList.add("active");
```

------

# ✅ 六、页面2：抽题动画（核心）

## 🎲 网格布局

```css
.grid {
  display: grid;
  grid-template-columns: repeat(10, 1fr);
  gap: 10px;
}
```

------

## 卡片闪烁

```css
.question-card {
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(0,229,255,0.2);
  height: 60px;
  animation: flicker 1.5s infinite alternate;
}

@keyframes flicker {
  from { opacity: 0.5; }
  to { opacity: 1; }
}
```

------

## 抽中高亮

```css
.selected {
  background: rgba(0,229,255,0.2);
  box-shadow: 0 0 20px #00E5FF;
  transform: scale(1.1);
}
```

------

## JS随机跳动（核心逻辑）

```js
let current = 0;
let timer = setInterval(() => {
  cards[current].classList.remove("selected");
  current = Math.floor(Math.random() * cards.length);
  cards[current].classList.add("selected");
}, 80);

setTimeout(() => {
  clearInterval(timer);
  // 最终停留
}, 2000);
```

------

# ✅ 七、页面3：倒计时（重点）

## 环形进度条

```css
.circle {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  border: 6px solid rgba(255,255,255,0.1);
  position: relative;
}

.circle-progress {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  border: 6px solid #00E5FF;
  clip-path: inset(0 50% 0 0);
  animation: countdown 30s linear forwards;
}

@keyframes countdown {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

------

## 数字跳动

```css
.timer {
  font-size: 48px;
  font-weight: bold;
  animation: pulse 1s infinite;
}

@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.1); }
  100% { transform: scale(1); }
}
```

------

# ✅ 八、选项点击波纹

```css
.option {
  position: relative;
  overflow: hidden;
}

.option::after {
  content: "";
  position: absolute;
  width: 100%;
  height: 100%;
  background: rgba(0,229,255,0.3);
  transform: scale(0);
  border-radius: 50%;
  transition: transform 0.4s ease;
}

.option:active::after {
  transform: scale(2);
}
```

------

# ✅ 九、页面4：分数动画（非常关键）

## 分数滚动

```js
function animateScore(el, start, end, duration = 800) {
  let startTime = null;

  function update(time) {
    if (!startTime) startTime = time;
    const progress = (time - startTime) / duration;
    const value = Math.floor(start + (end - start) * progress);

    el.innerText = value;

    if (progress < 1) {
      requestAnimationFrame(update);
    } else {
      el.innerText = end;
    }
  }

  requestAnimationFrame(update);
}
```

------

## +10 飘字

```css
.score-float {
  position: absolute;
  color: #00FFA3;
  animation: floatUp 1s ease forwards;
}

@keyframes floatUp {
  from {
    transform: translateY(0);
    opacity: 1;
  }
  to {
    transform: translateY(-40px);
    opacity: 0;
  }
}
```

------

# ✅ 十、右侧排行榜动画（重点）

## 排名变化

```css
.rank-item {
  transition: transform 0.5s ease;
}
```

------

## 上下移动（FLIP动画）

```js
function animateRank(list) {
  const first = list.map(el => el.getBoundingClientRect());

  // DOM排序变化

  const last = list.map(el => el.getBoundingClientRect());

  list.forEach((el, i) => {
    const dx = first[i].top - last[i].top;

    el.style.transform = `translateY(${dx}px)`;

    requestAnimationFrame(() => {
      el.style.transform = "";
    });
  });
}
```

------

# ✅ 十一、页面切换（科技感）

```css
.page-enter {
  opacity: 0;
  transform: translateY(20px);
}

.page-enter-active {
  opacity: 1;
  transform: translateY(0);
  transition: all 0.5s ease;
}
```

------

# ✅ 十二、开发落地建议（很关键）

### 动效节奏：

- 抽题：**最强**
- 倒计时：**稳定**
- 结算：**爆发**
- 排名：**持续微动**

------

### 性能建议：

- 粒子控制在50以内
- 动画尽量用 transform / opacity
- 避免频繁 reflow

------

# ✅ 最后一段（直接给开发）

> 页面整体采用深色科技风，基于CSS变量构建统一视觉体系，核心通过发光边框、粒子流、扫描动效和渐变动画强化AI控制台风格。关键交互（抽题、倒计时、结算）通过JS驱动动画（requestAnimationFrame + CSS animation）实现流畅体验，排行榜采用FLIP动画保证排序过渡自然。